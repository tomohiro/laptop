# vim: ft=yaml
---
resources:
  # Prepare
  - type: command
    script: apm upgrade --compatible --no-confirm

  # Atom packages
  <%- atom_packages.each do |package| -%>
  - type: atom_package
    name: <%= package %>
  <%- end -%>

  # Cleanup packages
  - type: command
    script: apm clean
