# vim:ft=yaml

resources:
  - type: template
    destination: /usr/local/etc/dnsmasq.conf
    source: templates/dnsmasq.conf
    notify:
      - restart_dnsmasq

handlers:
  - id: restart_dnsmasq
    type: command
    script: sudo brew services restart dnsmasq
