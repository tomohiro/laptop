# Configuration file for Dnsmasq.
#
# Format is one option per line, legal options are the same
# as the long options legal on the command line. See
# "/usr/sbin/dnsmasq --help" or "man 8 dnsmasq" for details.

port=53                   # Listen on the starndard DNS port
listen-address=127.0.0.1  # Listen on loopback address to allow query from local

domain-needed # Never forward plain names (without a dot or domain part)
bogus-priv    # Never forward addresses in the non-routed address spaces
no-resolv     # Don't want dnsmasq to read /etc/resolv.conf
no-poll       # Don't want dnsmasq to poll /etc/resolv.conf
no-hosts      # Don't want dnsmasq to read /etc/hosts

# Wildcard dev domain
address=/dev/127.0.0.1
