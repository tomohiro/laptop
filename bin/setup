#!/bin/sh

setup() {
  bundle install --path vendor/bundle
}

serverkit_apply() {
  local recipe=$1

  bundle exec serverkit apply "$recipe" \
    --variables=variables \
    --log-level=DEBUG
}

setup
serverkit_apply 'recipes/main_recipe.yml'
